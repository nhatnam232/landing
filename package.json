{
  "name": "blv-guard-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for BLV Guard Bot",
  "dependencies": {
    "mongodb": "^6.3.0",
    "dotenv": "^16.3.1"
  },
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static site, no build needed'"
  }
}
